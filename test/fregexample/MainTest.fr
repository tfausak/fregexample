module fregexample.MainTest where

import fregexample.Main ()

import Test.QuickCheck

main :: IO ()
main = do
    quickCheck $ once $ Main.collatz 0 == 0
    quickCheck $ once $ Main.collatz 1 == 1
    quickCheck $ once $ Main.collatz 2 == 1
    quickCheck $ once $ Main.collatz 3 == 10
